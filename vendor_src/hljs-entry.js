// highlight.js with its common languages, for the Builder post page's code
// blocks (builder_src/code.js). The editor highlights with lowlight, which is
// this same library, so the two give the same colours.
import hljs from 'highlight.js/lib/common'

window.hljs = hljs
